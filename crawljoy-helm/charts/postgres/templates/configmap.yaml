apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.configMaps.name }}
  namespace: {{ .Release.Namespace }}
data:
  POSTGRES_DB: {{ .Values.configMaps.config.postgresDB | quote }}
  POSTGRES_HOST: {{ .Values.configMaps.config.postgresHost | quote}}
